[pytest]
testpaths = tests
# The integration suite needs a live cluster, so it is deselected by default
# and run explicitly through `make e2e`.
addopts = -m "not integration"
markers =
    integration: asserts the live kind cluster; run with make e2e
